spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/foodly?currentSchema=foodly
    username: foodly
    password: password123
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: none # could be also create-drop
    show-sql: true



